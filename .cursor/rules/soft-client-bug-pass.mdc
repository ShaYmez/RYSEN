---
description: Soft-client stretch — tip-safe hot-path fixes (OBP activate gate, idx miss throttle, SC proxy reaper)
alwaysApply: true
---

# Soft-client / stretch notes

Tip = **`fdfbfa2`** + rewritten selfcare/parrot (`5f3f7e1`) + safe bloat cut (`c385a76`) + **stretch hot-path package** + **tip stability bug pass**:

| Fix | What |
|-----|------|
| OBP `activate_ua` | Call-start / new-stream **or** first inbound claim on outbound stub (`_obp_ua_arm`) — not every voice frame |
| UA key-up notify | Never `BRIDGE_SND` on timer-only UA refresh; notify only on ACTIVE/topology change |
| OBP empty-fi owner | When LoopControl `fi` is empty, continue as owner (do not hard-drop) — not full `50e2e62` |
| OBP stub harden | Outbound stubs + empty-fi: `setdefault` `LC`/`1ST`/counters; one-shot owner `LOOPLOG`; no `_sysslot` NameError |
| `BRIDGE_IDX` miss/stale | Rebuild coalesced (≥1s); stale hit path also rebuilds; full-scan fallback always |
| SC proxy | `MSTCL`×3 on reaper + RX idle reset; **no** `cleanup_peer` on mid-login `MSTNAK`; LoopingCall errback log-only |
| Diagnostics | ms logs for rebuild / options / report / rule_timer; reporting errback; ON-timer → DEBUG |

## Do not re-land without explicit user ask

| Item | Was tried |
|------|-----------|
| OBP mesh hygiene | `bcbfdfd` — stretch returned |
| Leftovers (RATE DROP / empty-fi / keyup) | `50e2e62` — stretch returned |
| Reactor `deferToThread` offload | `9b07fb2` non-parrot half |
| `GEN_STAT_BRIDGES: False` on UK/USA | USA unstable under OBP; leave True |

## Keep (on tip)

| Item | Why |
|------|-----|
| Selfcare four (rewrite) | dial clear, IPSC requeue, DISC persist, ping-timeout |
| Parrot 9990 private echo | group/PC in → private out; never TG9/OBP; slim fanout |
| Lazy `make_stat_bridge` + `_ensure_master_on_legs` | Cuts BRIDGE_IDX explosion |
| Slim `BRIDGE_SND` report legs | Less reactor pickle work |
| DIAL/StartRef=9 OPTIONS sanitize | Stops 26s OPTIONS thrash |
| `loopingErrHandle` log-only | Avoids TG blackouts (hblink + SC proxy) |
| Stretch hot-path package (above) | Align OBP with FreeDMR simplicity; stop proxy RX reap |
| UA key-up notify gate | Skip BRIDGE_SND on timer-only activate; avoid reactor stall on busy TGs |
| OBP empty-fi owner only | Continue routing when LoopControl fi empty; do not re-land full `50e2e62` |
| OBP stub packets / LC / 1ST | Init/guard counters + LC on outbound stubs so empty-fi continue cannot KeyError |
| Tip stability bug pass | Mid-login MSTNAK no-cleanup; stub-first UA arm; rule_timer DEBUG; CALL END zero-packet guard |

## Ops (UK / Greece)

Keep SYSTEM `GENERATOR` modest and proxy `DESTPORTEND` matched. Soft stretch = late/bursty HBP into client jitter (same TX as MMDVM). Correlate `(DIAGNOSTICS) Reactor lag` vs proxy `Removed`/`New client`. Retest BlueDV/Peanut/DVSwitch on local TG and 235/116 after deploy. Roll **USA first**, then UK after tip stability package.
